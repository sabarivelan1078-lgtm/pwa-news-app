import React from 'react';\nimport { Box, Tabs, Tab, useTheme, useMediaQuery } from '@mui/material';\nimport { FEED_SECTIONS } from '../../shared/constants';\n\ninterface NavTabsProps {\n  currentSection: number;\n  onSectionChange: (section: number) => void;\n}\n\nconst NavTabs: React.FC<NavTabsProps> = ({ currentSection, onSectionChange }) => {\n  const theme = useTheme();\n  const isTablet = useMediaQuery(theme.breakpoints.up('md'));\n\n  return (\n    <Box\n      sx={{\n        borderBottom: 1,\n        borderColor: 'divider',\n        overflow: 'auto',\n        '&::-webkit-scrollbar': { height: 6 },\n        '&::-webkit-scrollbar-track': { background: '#f1f1f1' },\n        '&::-webkit-scrollbar-thumb': { background: '#888', borderRadius: 3 }\n      }}\n    >\n      <Tabs\n        value={currentSection}\n        onChange={(e, newValue) => onSectionChange(newValue)}\n        variant=\"scrollable\"\n        scrollButtons=\"auto\"\n        sx={{\n          minHeight: 48,\n          '& .MuiTabs-scrollButtons': {\n            '&.Mui-disabled': { opacity: 0.3 }\n          }\n        }}\n      >\n        {FEED_SECTIONS.map((section, index) => (\n          <Tab\n            key={index}\n            label={section}\n            value={index}\n            sx={{\n              minWidth: 'auto',\n              px: 2,\n              fontSize: isTablet ? '0.95rem' : '0.85rem',\n              textTransform: 'none',\n              fontWeight: 500\n            }}\n          />\n        ))}\n      </Tabs>\n    </Box>\n  );\n};\n\nexport default NavTabs;\n"